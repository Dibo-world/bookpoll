version: "3.8"

services:
  # PostgreSQL DB
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: bookpoll
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest

  # auth-service
  auth-service:
    image: bodibo/auth-service:latest
    build: ./auth-service
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/bookpoll
      JWT_SECRET: bookpoll-super-secret-key-2026
    depends_on:
      - postgres

  # book-service
  book-service:
    image: bodibo/book-service:latest
    build: ./book-service
    ports:
      - "8002:8002"
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/bookpoll
    depends_on:
      - postgres

  # vote-service
  vote-service:
    image: bodibo/vote-service:latest
    build: ./vote-service
    ports:
      - "8003:8003"
    environment:
      DATABASE_URL: postgresql://postgres:password@postgres:5432/bookpoll
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
    depends_on:
      - postgres
      - rabbitmq

volumes:
  postgres_data: