# 📋 BookPoll 백엔드 스펙 확인 문서

## ✅ 현재 구현 상태 분석

### 1️⃣ 실시간 알림 통신 방식

**현재 방식: 폴링 (Polling)**

```
notify-service/main.py의 구현:
- WebSocket ❌ (미구현)
- SSE ❌ (미구현)  
- ✅ REST API 폴링 방식 (구현됨)
```

#### 현재 알림 API 엔드포인트
```
GET /api/v1/notify/messages
Header: X-User-Id: {userId}

응답:
{
  "success": true,
  "data": [
    {
      "userId": 1,
      "message": "'1984' 책에 새 투표가 도착했어요!",
      "eventType": "VOTE_CAST",
      "createdAt": "2026-06-13T10:30:00"
    },
    ...
  ],
  "message": "ok"
}
```

#### 사용 방식
**폴링으로 주기적으로 호출 (예: 3초 마다)**
```javascript
setInterval(() => {
  fetch('http://localhost:8004/api/v1/notify/messages', {
    headers: { 'X-User-Id': userId }
  })
  .then(res => res.json())
  .then(data => {
    // 새로운 알림 표시
  });
}, 3000);
```

#### 알림 유형 (eventType)
1. **VOTE_CAST**: 투표가 진행됨
   - 메시지: "'{bookTitle}' 책에 새 투표가 도착했어요!"
2. **BOOK_REGISTERED**: 새 책이 등록됨
   - 메시지: "새 책 '{bookTitle}'이 등록됐어요!"

#### 주의사항
- 인메모리 저장소 사용 (서비스 재시작 시 데이터 초기화)
- 향후 프로덕션에서는 데이터베이스로 이동 필요
- 향후 WebSocket/SSE로 개선 권장

---

### 2️⃣ 마이페이지 관련 API 엔드포인트

#### ✅ 도서 수정 API (구현됨)
```
PUT /api/v1/books/{book_id}
Header: X-User-Id: {userId}

요청:
{
  "title": "수정된 책 제목",
  "author": "수정된 저자",
  "description": "수정된 설명"
}

응답 (200):
{
  "success": true,
  "data": {
    "id": 5,
    "title": "수정된 책 제목",
    "author": "수정된 저자"
  },
  "message": "ok"
}

에러 응답:
- 404: 책을 찾을 수 없음
- 403: 본인이 등록한 책만 수정 가능
- 401: 인증 필요
```

#### ✅ 도서 삭제 API (구현됨)
```
DELETE /api/v1/books/{book_id}
Header: X-User-Id: {userId}

응답 (200):
{
  "success": true,
  "data": null,
  "message": "삭제 완료"
}

에러 응답:
- 404: 책을 찾을 수 없음
- 403: 본인이 등록한 책만 삭제 가능
- 401: 인증 필요
```

#### ❌ 나의 투표 기록 조회 API (미구현)
**현재 상태: 없음**

**필요한 API:**
```
GET /api/v1/votes/my-votes 또는 /api/v1/votes/history
Header: X-User-Id: {userId}

기대 응답 (원하는 형태):
{
  "success": true,
  "data": [
    {
      "pollId": 7,
      "pollTitle": "2026 최고의 책",
      "votedBookId": 2,
      "votedBookTitle": "1984",
      "votedAt": "2026-06-13T10:30:00"
    },
    ...
  ],
  "message": "ok"
}
```

**개발자에게 요청사항:**
- vote-service에 `/api/v1/votes/my-votes` 엔드포인트 추가 필요
- 해당 userId의 VoteLog를 모두 조회하고, 각 투표 정보와 책 정보를 함께 반환

---

### 3️⃣ 마이페이지 UI 위치 (추천 방안)

#### 🎯 추천 방안: dashboard.html에 탭 추가

**구조:**
```
사이드바 메뉴 (기존)
├── 📖 도서 관리
├── 📊 투표 관리
└── 👤 마이페이지 (새로 추가)

메인 영역 (탭별로 전환)
├── section-book: 도서 목록
├── section-vote: 투표 관리
└── section-mypage: 마이페이지 (새로 추가)
```

**마이페이지 내용:**
```
1️⃣ 내가 등록한 도서
   ├── 도서 카드 (제목, 저자, 설명)
   ├── 수정 버튼 → 모달에서 책 정보 수정
   └── 삭제 버튼 → 책 삭제

2️⃣ 나의 투표 기록
   ├── 투표 이력 목록 (어느 투표에, 어느 책에 투표했나)
   ├── 투표 일시 표시
   └── 투표 결과 보기 버튼 → 해당 투표 결과 조회
```

**장점:**
- ✅ dashboard.html 한 페이지에서 모든 기능 관리
- ✅ 사이드바로 간편하게 전환
- ✅ 탭 구조로 일관성 있음
- ✅ notify.html은 알림 전용으로 분리 가능

---

## 📝 백엔드에 요청해야 할 작업

### 필수 구현 (프론트엔드 개발 전에)

| 기능 | API | 상태 | 우선순위 |
|------|-----|------|----------|
| 나의 투표 기록 조회 | `GET /api/v1/votes/my-votes` | ❌ 미구현 | 🔴 높음 |
| 도서 수정 | `PUT /api/v1/books/{id}` | ✅ 구현됨 | - |
| 도서 삭제 | `DELETE /api/v1/books/{id}` | ✅ 구현됨 | - |
| 알림 조회 | `GET /api/v1/notify/messages` | ✅ 구현됨 | - |

### 선택 구현 (나중에)

| 기능 | API | 현재 | 개선안 |
|------|-----|------|--------|
| 실시간 알림 | WebSocket/SSE | 폴링 | 성능 개선용 |
| 내가 등록한 책 필터링 | - | 전체 목록에서 필터링 | API에서 필터링 |

---

## 🔧 프론트엔드 개발 전략

### Phase 1: 현재 상태로 개발 가능
- ✅ 도서 등록, 수정, 삭제
- ✅ 투표 생성, 참여, 결과 조회
- ✅ 알림 폴링으로 실시간 표시 (3초마다)

### Phase 2: 나의 투표 기록 추가
- ⏳ `/api/v1/votes/my-votes` 백엔드 구현 필요
- 그 후 마이페이지에 투표 기록 탭 추가

### Phase 3: 성능 개선 (나중)
- WebSocket 또는 SSE로 알림 개선
- 폴링 제거

---

## 🎨 마이페이지 UI 프로토타입

```
┌─────────────────────────────────────────┐
│           👤 마이페이지                   │
├─────────────────────────────────────────┤
│ [내가 등록한 도서] [나의 투표 기록]       │
├─────────────────────────────────────────┤
│                                           │
│  📚 내가 등록한 도서 (현재 탭)             │
│                                           │
│  ┌──────────────────────────────────┐   │
│  │ 1984                  [수정] [삭제]  │
│  │ George Orwell                   │   │
│  │ 디스토피아 소설                    │   │
│  └──────────────────────────────────┘   │
│                                           │
│  ┌──────────────────────────────────┐   │
│  │ 동물농장                 [수정] [삭제] │
│  │ George Orwell                   │   │
│  │ 풍자 소설                         │   │
│  └──────────────────────────────────┘   │
│                                           │
├─────────────────────────────────────────┤
│  📊 나의 투표 기록 (다른 탭)               │
│                                           │
│  ┌──────────────────────────────────┐   │
│  │ 투표명: 2026 최고의 책            │   │
│  │ 투표 선택: 1984                   │   │
│  │ 투표 일시: 2026-06-13 10:30      │   │
│  │                [결과 보기]         │   │
│  └──────────────────────────────────┘   │
│                                           │
│  ┌──────────────────────────────────┐   │
│  │ 투표명: 동물농장 vs 1984          │   │
│  │ 투표 선택: 동물농장               │   │
│  │ 투표 일시: 2026-06-12 15:45      │   │
│  │                [결과 보기]         │   │
│  └──────────────────────────────────┘   │
│                                           │
└─────────────────────────────────────────┘
```

---

## 🚀 프론트엔드 구현 로드맵

### 즉시 구현 가능 (현재 코드로)
1. ✅ dashboard.html에 마이페이지 섹션 추가
2. ✅ 내가 등록한 도서 필터링 및 수정/삭제 버튼
3. ✅ 알림 폴링 (3초마다 GET /api/v1/notify/messages)

### 백엔드 구현 대기
4. ⏳ `/api/v1/votes/my-votes` API 구현 완료 후 마이페이지에 투표 기록 추가
5. ⏳ 도서 수정 모달 구현

### 선택 구현 (나중)
6. 🔔 WebSocket/SSE로 실시간 알림 개선

---

## 📞 개발 우선순위

### 1단계 (지금 바로)
- [ ] 마이페이지 UI 틀 만들기 (빈 탭)
- [ ] 내가 등록한 도서 필터링 & 표시
- [ ] 수정/삭제 버튼 추가

### 2단계 (백엔드 준비되면)
- [ ] 나의 투표 기록 조회 API 통합
- [ ] 도서 수정 모달 구현

### 3단계 (향후)
- [ ] 알림 WebSocket/SSE 개선
- [ ] 투표 결과 모달

